id: fix_syntax_error
description: "Fix the syntax error in app.py so it runs without errors"
category: bug_fix
setup:
  - "echo 'def greet(name\n    print(f\"Hello {name}\")' > app.py"
expected_files:
  - app.py
validation:
  - python app.py
timeout_s: 90
tags: [bug_fix, python, syntax]
